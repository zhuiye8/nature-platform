# 业务规则确认记录 — Nature 等保测评平台

> 本文档记录所有经团队确认的业务规则细节，避免重复讨论。
> 最后更新：2026-05-07

---

## 1. 合同生命周期

### 状态流转

```
DRAFT → SUBMITTED → APPROVED → ARCHIVED
                  → REJECTED → DRAFT（驳回后可重新编辑提交）
```

### 已确认规则

| 规则 | 结论 | 确认日期 |
|------|------|---------|
| 合同归档后是否可编辑 | **可以编辑**（商务可继续补充归档字段） | 2026-03-23 |
| 合同表单是否支持草稿自动保存 | **不支持**，一次性提交 | 2026-03-23 |
| 合同编号生成时机 | 审核通过后自动生成，驳回后重新通过会重新生成 | 2026-03-23 |
| 合同删除范围 | 仅创建人在 DRAFT 状态可删除，进回收站 | 2026-03-23 |

---

## 2. 质量审核

### 已确认规则

| 规则 | 结论 | 确认日期 |
|------|------|---------|
| 驳回时是否必须填写原因 | **必填**（wf_task.remark NOT NULL on REJECT） | 2026-03-23 |
| 驳回后现场测评数据是否清空 | **不清空**，保留修改，测评师在原有基础上修正 | 2026-03-23 |
| 一个用户能否同时是 PM 和技术审核 | **可以**（不同项目可以，同项目内通过回避机制处理） | 2026-03-23 |
| 同一用户同一项目能否既是 PM 又是审核人 | **不可以**（回避规则 SAME_PROJECT 自动排除） | 2026-03-23 |

### 回避算法

```
输入：node_key, slot_key, project_id
输出：assignee_id（选中的审核人）

步骤：
1. 读取 wf_assignment_rule WHERE node_key AND slot_key，按 priority ASC 排序
2. 对每条规则，读取该 role_code 的所有用户（按 user_role.sort_order ASC）
3. 如果 avoidance_rule = 'SAME_PROJECT'：
   → 读取 project_member WHERE project_id AND status='ACTIVE' → 回避名单
   → 从用户列表中排除回避名单
4. 取排除后的第一个用户
5. 如果当前角色无可用人选，尝试下一条规则（更低优先级的角色）
6. 全部规则都无人可选 → 返回 null，前端提示管理员手动指定
```

---

## 3. 现场测评

### 已确认规则

| 规则 | 结论 | 确认日期 |
|------|------|---------|
| 多人协作模式 | PM + 测评师各自提交各自部分，全员提交→节点完成 | 2026-03-23 |
| 发起质量审核的入口 | 在现场测评页面内，仅 PM 可见的按钮 | 2026-03-23 |
| 质量审核驳回返回现场测评 | 保留已有数据，测评师修改后重新提交 | 2026-03-23 |

---

## 4. 角色与权限

### 已确认规则

| 规则 | 结论 | 确认日期 |
|------|------|---------|
| 部门是否参与权限判定 | **不参与**，仅用于展示和筛选 | 2026-03-23 |
| 一个用户可以有多个角色 | **可以**（如某人既是测评师又是报告编制员） | 2026-03-23 |
| 角色内用户排序 | user_role.sort_order 决定自动选人优先级 | 2026-03-23 |
| 合同详情可见性 | 创建人看全部；其他销售能进详情但看部分字段；商务/管理员看全部 | 2026-03-23 |
| **PM 资格如何授权** | **独立 `project_manager` 角色，由项目主管手工授予；与测评师等级解耦**（中级测评师不一定能担任 PM）| 2026-04-29 |
| PM 候选池过滤逻辑 | DIRECTOR_REVIEW 节点：候选 = `project_manager` 角色用户 ∩ senior/middle_assessor，按等级分组 | 2026-04-29 |
| 董事长（chairman）角色 | 预留角色，权限/资源由管理员后台手工配置（默认仅 dashboard） | 2026-04-29 |
| 财务（finance）角色 | 独立菜单"财务管理"（合同财务/开票申请/费用请款/结算管理） | 2026-04-29 |

---

## 5. 回收站

### 已确认规则

| 规则 | 结论 | 确认日期 |
|------|------|---------|
| 接入回收站的业务类型 | **CONTRACT / CONTRACT_GROUP / PROJECT_REGISTER / PLATFORM** 共 4 种 | 2026-05-07 |
| 软删除统一入口 | **业务侧必须调用 `RecycleService.softDelete(opts)`**，事务内执行业务侧 applyDelete 回调 + INSERT recycle_bin。新增软删表必须遵循此约定 | 2026-05-07 |
| 子记录处理 | 跟随父记录恢复（不单独入回收站）。例如恢复 contract 时连带恢复 contract_system_item | 2026-05-07 |
| 回收站保留期限 | **永久保留**，无自动清理；只能由 super_admin 手动永久删除 | 2026-03-23 |
| 恢复/永久删除权限 | 仅 super_admin（默认 chairman 不开放） | 2026-05-07 |
| 恢复唯一性校验 | CONTRACT: contract_no 不冲突；PROJECT_REGISTER: contract_id+contract_year 不冲突；CONTRACT_GROUP/PLATFORM 无校验 | 2026-05-07 |
| 永久删除行为 | 物理 DELETE 原表（含子记录）+ 删 recycle_bin 记录。**不释放 ID/业务编号**（PG sequence 单调递增固有行为） | 2026-05-07 |
| file_attachment 是否进回收站 | **不进**。文件级软删属工程实现（覆盖上传/换附件等场景），非业务"误删恢复"语义 | 2026-05-07 |

---

## 6. 工作流通用规则

### 已确认规则

| 规则 | 结论 | 确认日期 |
|------|------|---------|
| 流程拆分 | 4 个独立流程：合同（A）/ 项目测评（B）/ 开票申请（FIN_INVOICE）/ 费用请款（FIN_EXPENSE） | 2026-04-29 |
| 审核驳回必须填写原因 | **所有 REVIEW 节点驳回时 remark 必填** | 2026-03-23 |
| 运行中实例的版本管理 | 绑定创建时的 definition_id，不受新版本影响 | 2026-03-23 |
| 新流程扩展方式 | INSERT wf_definition + wf_node + wf_transition，零代码变更 | 2026-03-23 |
| seed.sql 重复执行 | **必须幂等** — 所有绑定表都有 unique 约束保护（migration 0016 修复历史 bug） | 2026-04-29 |

---

## 7. 通知规则

### 已确认规则

| 事件 | 通知对象 | 确认日期 |
|------|---------|---------|
| 合同审核通过 | 拥有商务权限的全部用户 | 2026-03-23 |
| 合同归档完成 | 合同创建人 | 2026-03-23 |
| 审核节点进入（有 assignee） | 仅 assignee | 2026-03-23 |
| 审核节点进入（无 assignee） | 该角色池全部用户 | 2026-03-23 |
| 质量审核驳回 | 项目经理 | 2026-03-23 |
| 报告审核驳回 | 报告编制人 | 2026-03-23 |

---

## 8. 文件管理规则

### 已确认规则

| 规则 | 结论 | 确认日期 |
|------|------|---------|
| 单文件上限 | 500MB | 2026-03-23 |
| 分片大小 | 5MB/片 | 2026-03-23 |
| 预签名 URL 有效期 | 30 分钟 | 2026-03-23 |
| 跨节点可见 | 查询 biz_type + biz_id 不过滤 node_key | 2026-03-23 |
| 文件保留策略 | 永久保留（合规要求） | 2026-03-23 |

---

## 9. 时间规则

### 已确认规则

| 规则 | 结论 | 确认日期 |
|------|------|---------|
| 业务时区 | 统一 Asia/Shanghai | 2026-03-23 |
| 数据库存储 | TIMESTAMPTZ（UTC） | 2026-03-23 |
| 前端展示 | 强制 CST 格式化 | 2026-03-23 |
| 编号日期 | Asia/Shanghai 日期 | 2026-03-23 |

---

## 10. 待未来确认（不阻塞 V1 开发）

| 问题 | 状态 | 影响阶段 |
|------|------|---------|
| 钉钉 OAuth 应用配置（AppKey/AppSecret） | 待运维提供 | Phase 1 后期 |
| 报告模板具体变量映射 | 待业务确认 | Phase 3 |
| 离线终端 manifest.json 完整 schema | 待设计 | Phase 4 |
| 等保 2.0 标准库数据结构 | 待提供现场测评数据库 | Phase 4 |
| 质量审核 SLA（多久必须完成） | 暂不设限，未来可配置 | V2 |
