# NEXT_STEPS_P1

## 1. 目标
在已完成 P0（权限治理 + 角色池）的基础上，完成 P1 稳定性收口：
- 全链路自动化回归（API + UI E2E）
- 时间与通知一致性巡检与修复

## 2. 开发任务

### 2.1 E2E 自动化（优先）
当前进度：
- 已完成正向链路 01：合同审核通过 -> 合同归档 -> 项目登记
- 已完成正向链路 02：项目登记审核 -> 公安登记 -> 现场测评（ZIP + 4 人分配）-> 进入报告技术审核
- 已完成正向链路 03：技术审核 -> 内容审核 技术/管理/网络 -> 编制分配 -> 报告编制提交
- 已完成正向链路 04：最终审核 -> 材料归档
- 已完成异常链路：非超管恢复拦截、分配冲突提示、通知未读删除实时扣减
- 已完成异常链路：内容审核驳回后重提恢复（节点 12 reject/recovery）
- 已完成异常链路：技术审核驳回后重提恢复（节点 11 reject/recovery）
- 已完成异常链路：最终审核驳回后重提恢复（节点 15 reject/recovery）
- 已完成异常链路：通知触发审计 + 前端 Asia/Shanghai 时间格式代码巡检
- 已落地 API 执行结果 JSON 归档（`deploy/e2e/reports/api`）
- 已落地 UI 冒烟基线（Playwright）：登录与关键页面可达性
- 已完成 UI 冒烟增强：现场测评分配弹窗四下拉候选池绑定 + 未选齐阻断
剩余最小任务：
1. 无（P1 E2E 最小闭环已完成）

### 2.2 API 回归
当前进度：
- 已覆盖“归档前不可登记、归档后可登记”
- 已覆盖“回收站恢复冲突（contract_id + contract_year）”
- 已覆盖“通知删除未读后实时扣减”
- 已覆盖“内容审核驳回 -> 重提 -> 恢复推进”
- 已覆盖“技术审核驳回 -> 重提 -> 恢复推进”
- 已覆盖“最终审核驳回 -> 重提 -> 恢复推进”
剩余最小任务：
1. 无（P1 API 回归闭环已完成）

### 2.3 时间与通知一致性
当前进度：
- 已覆盖合同审核通过/归档通知触发校验
- 已覆盖通知列表与未读计数一致性校验
- 已核对 `time.ts` 采用 `Asia/Shanghai` + `+08:00` 归一化
- 已完成前端页面级时间渲染抽样清单（仪表盘、回收站、流程页）
剩余最小任务：
1. 无（P1 时间与通知一致性最小闭环已完成）

## 3. Definition of Done（当前状态）
1. `mvn -q test` 通过
2. `pnpm build` 通过
3. API E2E 全量（exception + happy-path 01~04）可本地复现
4. UI 冒烟 `pnpm test:e2e` 可本地复现
5. 文档同步并通过 format-doc 检查

## 5. 下一步最小任务（P2 候选）
1. 将 `deploy/e2e/api/run-all.ps1` 与 `pnpm test:e2e` 接入 CI，统一产出构建工件与 JSON/HTML 报告。
2. 为时间渲染补充截图比对或 DOM 文本快照断言，增强前端回归灵敏度。

## 4. 交付物
1. 变更清单（脚本 + 迁移 + 文档 + Playwright 基线）
2. 回归执行报告（通过/失败、关键日志结论）
3. 风险与下一步最小任务列表

