# API E2E 执行报告（2026-02-12）

## 1. 执行命令
```powershell
powershell -ExecutionPolicy Bypass -File C:\work\nature\codex\deploy\e2e\api\run-all.ps1
```

## 2. 结果总览
- 异常链路：通过
  - `reviewer_candidates_four_groups`
  - `non_admin_restore_forbidden`
  - `assignment_conflict_message`
  - `notification_seed_visible`
  - `notification_unread_count_increase_after_seed`
  - `notification_delete_success`
  - `notification_unread_count_decrement_realtime`
  - `tech_task_reject_success`
  - `tech_recovery_to_content_review`
  - `content_task_reject_success`
  - `content_status_rejected_after_reject`
  - `content_recovery_to_compile_assign`
  - `final_task_reject_success`
  - `final_recovery_to_material_archive`
  - `contract_approve_notification_event_present`
  - `contract_archive_notification_event_present`
  - `frontend_time_formatter_uses_asia_shanghai`
- 正向链路：通过
  - `happy-path-01`（合同审核 + 归档 + 项目登记）
  - `happy-path-02`（项目登记 -> 公安登记 -> 现场测评 ZIP + 4 人分配）
  - `happy-path-03`（技术审核 -> 内容审核 技术/管理/网络 -> 编制分配 -> 编制提交）
  - `happy-path-04`（最终审核 -> 材料归档）

## 3. 关键接口断言
1. `GET /api/v1/on-site-assessments/reviewer-candidates`
   - 断言：`200` 且返回四组候选人数组
2. `POST /api/v1/recycle-bin/PROJECT_REGISTER/{id}/restore`
   - 断言：非超管返回 `403`
3. `PUT /api/v1/quality-reviews/{projectId}/assignment`
   - 断言：并发冲突返回 `409` 且提示“任务已被分配，请刷新”
4. `POST /api/v1/on-site-assessments/{projectId}/submit`（自动入内容审核待办） + `POST /api/v1/workflow/tasks/{taskId}/reject`
   - 断言：驳回后内容审核状态 `REJECTED`
5. `POST /api/v1/on-site-assessments/{projectId}/submit`（重提） + 三个 `approve`
   - 断言：重提并审批后推进到 `REPORT_COMPILE_ASSIGN`
6. `POST /api/v1/report-compile-submissions/{projectId}/submit`
   - 断言：节点推进到 `REPORT_FINAL_REVIEW`
7. `POST /api/v1/workflow/tasks/{taskId}/reject`（技术/最终审核）
   - 断言：可驳回至 `REJECTED`，重提并审批后恢复推进
8. `POST /api/v1/material-archives/{projectId}/submit`
   - 断言：归档状态 `ARCHIVED`，`workflowStatus=APPROVED`
9. `DELETE /api/v1/notifications/{id}` + `GET /api/v1/notifications/unread-count`
   - 断言：删除未读通知后，未读计数实时减 1
10. `GET /api/v1/notifications?limit=200`
   - 断言：合同审核通过/归档事件类型出现在目标用户通知列表

## 4. 关键日志结论
- 内容审核驳回恢复链路已具备自动回归能力（reject -> resubmit -> approve all）。
- 技术审核、最终审核驳回恢复链路已补齐并可自动回归。
- API E2E 结果已自动落盘：`deploy/e2e/reports/api/*.json`，可直接用于 CI 排障。
- 历史故障修复仍有效：`workflow_action_log.action` 扩容后，节点 12/13/14/15 链路稳定通过。

## 5. 本轮补充验证
- 后端：`mvn -q test` 通过
- 前端：`pnpm build` 通过
- UI 冒烟：`pnpm test:e2e` 通过（4/4）
- 文档检查：`check_format_doc.py --root C:\work\nature\codex --mode changed --allow-missing-architecture` 通过

